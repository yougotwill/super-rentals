// Instead of copying the entire controller we can just import and re-export 
// the rentals controller as the rentals/index controller
import RentalsController from '../rentals';

export default RentalsController;